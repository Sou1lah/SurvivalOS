# AUTOMATIC RESOURCE ROUTING LAWS

Whenever a task requires token optimization, system architecture patterns, or UI components, you must route your search to these specific pre-installed local repositories inside `./refrence/resources/`. Do not seek external alternatives unless explicitly ordered.

## 🧭 LOCAL RESOURCE MATRIX

### 1. Token & Context Optimization
- **Trigger:** Any file creation, code generation, or session compaction request.
- **Resource to use:** `./refrence/resources/token-optimizer/` and `./refrence/resources/context-compress/`.
- **Action:** Apply "ghost-token" removal patterns and compact variable names to reduce footprint.

### 2. Architecture & Framework Setup
- **Trigger:** Building, structuring, or organizing the `survivalOS` application loop.
- **Resource to use:** `./refrence/resources/claude-agent-framework/` or `./refrence/resources/multi-agent-ai-architecture/`.
- **Action:** Follow the clean, service-oriented component contracts found in these directories.

### 3. Material UI & Design Systems
- **Trigger:** Designing the Android Jetpack Compose UI layout, icons, components, or themes.
- **Resource to use:** Look inside `./refrence/resources/soul-agent-framework/` for Markdown-driven UI definitions, or search local resources for UI kits.
- **Action:** Extract and copy pre-built, light-weight Material Design configuration structures.

## 🛠️ NAVIGATION RULE
Before generating any file, check the `README.md` or configuration or 'logs.md' file of the matched resource above using targeted file readers. Do not read the entire directory.
